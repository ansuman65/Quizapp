<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <!-- Timer -->
        <TextView
            android:id="@+id/timerText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Time Left: 45s"
            android:textSize="14sp"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="6dp"/>

        <!-- Counter -->
        <TextView
            android:id="@+id/questionCounter"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Question 1 of 5"
            android:textSize="14sp"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="10dp"/>

        <!-- Question -->
        <TextView
            android:id="@+id/questionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Question"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:gravity="center"
            android:padding="20dp"
            android:layout_marginBottom="10dp" />

        <!-- Options Grid -->
        <GridLayout
            android:id="@+id/optionsGrid"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:columnCount="2"
            android:rowCount="2"
            android:alignmentMode="alignMargins"
            android:useDefaultMargins="true"
            android:layout_marginBottom="20dp">

            <Button
                android:id="@+id/option1"
                style="@style/QuizButtonRed"
                android:text="Option A" />

            <Button
                android:id="@+id/option2"
                style="@style/QuizButtonOrange"
                android:text="Option B" />

            <Button
                android:id="@+id/option3"
                style="@style/QuizButtonBlue"
                android:text="Option C" />

            <Button
                android:id="@+id/option4"
                style="@style/QuizButtonPurple"
                android:text="Option D" />

        </GridLayout>


        <!-- Next Button -->
        <Button
            android:id="@+id/nextButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Next"
            android:textAllCaps="false"
            android:textColor="@color/button_text_light"
            android:textSize="16sp"
            style="@style/QuizButtonGreen"/>

    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
